{
  "name": "mikrotik-monitor-monorepo",
  "version": "1.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces --if-present",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:push": "npm run db:push -w apps/api",
    "db:generate": "npm run db:generate -w apps/api",
    "seed:dummy": "npm run seed:dummy -w apps/api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "es-abstract": "^1.24.1",
    "es-errors": "^1.3.0",
    "es-object-atoms": "^1.1.1",
    "gopd": "^1.2.0",
    "math-intrinsics": "^1.1.0",
    "pg": "^8.20.0",
    "postgres": "^3.4.7",
    "tsx": "^4.21.0"
  }
}
