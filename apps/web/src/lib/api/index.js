// Export API client
export { apiClient, get, post, put, del } from './client';

// Export all services
export {
    routerService,
    userService,
    alertService,
    groupService,
    settingsService,
} from './services';
